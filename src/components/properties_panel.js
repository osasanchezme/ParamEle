import state from "../state";

function PropertiesPanel({visible, width}){
    return(
        <div className="properties-panel" style={{width: width + "%", background: "black"}}>

        </div>
    )
}

export default PropertiesPanel;